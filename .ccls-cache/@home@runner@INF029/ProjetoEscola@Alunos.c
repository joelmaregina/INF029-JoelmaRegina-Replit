#include <stdio.h>
#include "Alunos.h"

Aluno mainAluno()
{
  
  printf("============ CADASTRO DE ALUNOS ============ \n");
  // printf("Digite o número de matrícula: \n");
  // fgets(alunoAtual[i].matricula, TAMMATRICULA, stdin);
  
  // printf("Digite o seu nome: \n");
  // fgets(alunoAtual[i].nome, TAMNOME, stdin);
  
  // printf("Digite seu sexo: \n");
  // scanf("%c", &alunoAtual[i].sexo);
  
  // printf("Digite sua data de nascimento DD MM AAAA : \n");
  // scanf("%d %d %d", &alunoAtual[i].nascimento.dia, &alunoAtual[i].nascimento.mes, &alunoAtual[i].nascimento.ano);
  // getchar();
  
  // printf("Digite seu CPF: \n");
  // fgets(alunoAtual[i].cpf, TAMCPF, stdin);
  // getchar();
  
}